namespace CalculatorLibrary
{
    public static class CalculatorClass
    {
        //i guess can be static since i don't need anything from instance.  that is why the warnings.
        public static int Add(int x, int y) => x + y;
        public static int Subract(int x, int y) => x - y;
    }
}